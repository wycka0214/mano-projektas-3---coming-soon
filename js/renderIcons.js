function renderIcons ( icons ) {
    const count = icons.lenght;

    console.log( 'is viso bus', count );

    for ( let i=0; i<count; i++ ) {
        console.log('icon:', i);
    }
        

    return;
}

export default renderIcons;